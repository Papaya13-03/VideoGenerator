# Frontend (Next.js)

Web app cho VideoGenerator — sẽ dựng ở **Pha 3** (Next.js + TS + Tailwind + shadcn/ui).
Giao tiếp với backend qua REST API (`/api/v1`) + SSE cho tiến độ render.

Chưa khởi tạo. Xem `../PLAN.md` mục B5/B6.
